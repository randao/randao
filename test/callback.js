var Timecop = require('./helper/Timecop');

contract('Randao#callback', function(accounts) {

  it("other contract register a callback and send a bounty");

  it("other contract get called after random number generated");
});
